# מה נשאר לעשות ביד, ובאיזה סדר

> **למי:** לבעל הפרויקט. שלושה דברים שאי אפשר לעשות מהסביבה הזו, כי הם דורשים התחברות לחשבונות שלך.
> עודכן: 25.9.2026

**ההחלטה (25.9, מעדכנת את זו מ-23.9):** האתר עובר לחשבון העסקי. **הבוט האישי נשאר איפה שהוא** — הוא שירות חי עם שלושה וובהוקים חיצוניים, הוא באמת אישי, ושום דבר בפרויקט לא דורש שיזוז. החיבור בין הבוט לאתר חד-כיווני וחוצה חשבונות בלי בעיה.

---

## 1. האתר → החשבון העסקי

**למה דווקא הוא:** תוכנית Hobby של Vercel חוסמת שימוש מסחרי, **וגם חוסמת פונקציות ב-60 שניות**. `api/staff/voice` מוגדר ל-90. כדאי לבדוק מה התוכנית של החשבון הנוכחי — אם הוא Hobby, ייתכן שדיווח קולי ארוך כבר נחתך בשקט.

| | מה |
|---|---|
| 1 | **GitHub:** להעביר בעלות על `roshinovic2/levi-garage-ai` לחשבון העסקי, ואז מקומית: `git remote set-url origin <הכתובת החדשה>` |
| 2 | **Vercel:** ליצור פרויקט חדש בחשבון העסקי **עם קישור לגיט** (בניגוד להיום, שכל פריסה ידנית) |
| 3 | להגדיר שלושה משתנים ב-production: `GEMINI_SA_JSON`, `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` |
| 4 | לעדכן את הכתובת החדשה ב-`03-rollout/website/README.md` וב-`STATUS.md` |

⚠️ **מלכודת שכבר הפילה אותנו פעם:** ב-`.env.local` יש הערות באותה שורה של הערך. חילוץ נאיבי גורר את ההערה לתוך הערך ושובר את האתר.

---

## 2. הבוט ✅ נדחף וחי (25.9)

**הקומיט הישן (`d4e2df7`) לא נדחף.** הוא נכתב על עותק מקומי ישן בשבועיים, ובמאגר חיכו 30 קומיטים חדשים — כולל קבוצה משותפת ותור קבצים אישי. דחיפה בכוח הייתה מוחקת את כולם. הוא שמור בענף `backup/garage-routing-v1`, והענף נכתב מחדש על הקוד העדכני ונדחף כ-`801a8a0`. הבוט פרס את עצמו מ-main, וה-CI ירוק (18 סוויטות, 862 בדיקות).

**איך הדחיפה עברה בלי דפדפן:** ה-GitHub CLI במחשב כבר מחובר לשלושה חשבונות, כולל `roshinovic-roi`. הדחיפה עברה עם האישור השמור שלו, והחשבון הפעיל הוחזר ל-`roshinovic2`. בנוסף, כתובת המאגר כוללת עכשיו את שם המשתמש (`https://roshinovic-roi@github.com/...`), כך שגם `git push` רגיל ישאל על החשבון הנכון.

---

## 3. משתני הסביבה ✅ הוגדרו בשני הצדדים (25.9)

שני הצדדים הוגדרו, והאתר נפרס. **אחרי העברת האתר לחשבון העסקי** מריצים שוב את שני הצדדים עם ה-`SITE_URL` החדש: הסקריפט מוחק ומגדיר מחדש, והטוקן נשמר בקובץ המקומי. ההוראות למטה נשארות לשם כך.

⚠️ **מלכודת שעלתה לנו:** במחשב היה מוגדר `VERCEL_TOKEN` כמשתנה סביבה קבוע של Windows, והוא גבר בשקט על כל `vercel login`. הוא נמחק ב-25.9.

יש סקריפט שעושה את כל זה בפקודה אחת לכל צד, **בלי שאף ערך יודפס או ייכנס לריפו**:

### מה שצריך ממך פעם אחת

**א. חיבור ל-Vercel.** אחת משתי דרכים:

```
$env:NODE_OPTIONS = "--require C:\projects\final-project\levi-garage\scripts\ascii-hostname.cjs"
npx vercel login
Remove-Item Env:NODE_OPTIONS
```

⚠️ **למה השורה הראשונה:** שם המחשב הוא "אלון", וה-CLI של Vercel שולח אותו בכותרת HTTP כשהוא יוצר את טוקן ההתחברות. כותרות HTTP מקבלות רק תווים לטיניים, ולכן `npx vercel login` רגיל נופל עם `is not a legal HTTP header value`. [הקובץ הזה](../../levi-garage/scripts/ascii-hostname.cjs) מחליף את השם ל-`alon-pc` רק בתוך הפקודה הזו, ושם המחשב עצמו לא משתנה. אחרי ההתחברות הוא לא נחוץ: שאר הפקודות לא שולחות את שם המחשב.

או, אם אתה מעדיף שאעבוד בלי שתהיה מחובר: ליצור טוקן ב-Vercel (Settings → Tokens) ולשים אותו **במשתנה סביבה** `VERCEL_TOKEN`, לא בצ'אט.

**ב. קובץ מקומי אחד** בשם `.env.wiring.local`, בתיקייה `03-rollout/solution-3-agent/`. הוא כבר ב-`.gitignore`:

```
SITE_URL=https://levi-garage.vercel.app
NEXT_PUBLIC_WHATSAPP_NUMBER=9725XXXXXXXX
BOT_DIR=C:\projects\Personal-Bot
BOT_URL=https://<הכתובת של הבוט>.vercel.app
```

- `SITE_URL` — הכתובת **אחרי** ההעברה לחשבון העסקי, בלי `/` בסוף.
- `NEXT_PUBLIC_WHATSAPP_NUMBER` — המספר שאליו הוואטסאפ באמת מגיע, ספרות בלבד עם קידומת מדינה. **זה המספר האמיתי היחיד בכל המערך**, ולכן הוא חי רק כאן וב-Vercel.
- `BOT_URL` — הכתובת של הבוט, שאליה האתר פונה כדי לשלוח "הרכב מוכן".
- `GARAGE_BOT_TOKEN`, `GARAGE_NOTIFY_TOKEN` — **לא למלא.** הסקריפט מייצר אותם בהרצה הראשונה, שומר אותם באותו קובץ, ומשתמש באותו ערך בשני הצדדים.

### ההרצה

```
node 03-rollout/solution-3-agent/scripts/wire-garage.mjs site
node 03-rollout/solution-3-agent/scripts/wire-garage.mjs bot
```

| צד | מה מוגדר |
|---|---|
| **האתר** | `GARAGE_BOT_TOKEN` · `NEXT_PUBLIC_WHATSAPP_NUMBER` · `GARAGE_NOTIFY_URL` · `GARAGE_NOTIFY_TOKEN` |
| **הבוט** | `GARAGE_ASK_URL` · `GARAGE_BOT_TOKEN` · `GARAGE_BOOKING_URL` · `GARAGE_PHONE` · `GARAGE_NOTIFY_TOKEN` |

אחרי כל צד צריך פריסה מחדש, כי משתנה סביבה נכנס לתוקף רק בבנייה הבאה. הסקריפט מדפיס את הפקודה המדויקת.

**למה הטוקן בכלל:** בלעדיו, `api/ask` באתר מגביל קצב **לפי כתובת IP**. כל הודעות הוואטסאפ מגיעות מאותו שרת, ולכן לקוח אחד עמוס היה חוסם את כל השאר. עם הטוקן, האתר סופר לכל לקוח בנפרד, לפי HMAC של המספר עם הטוקן כמפתח — כך שהמספר עצמו לא מגיע לאתר.

**`GARAGE_PHONE` הוא המספר הבדוי** `04-0000000`, והוא כתוב בסקריפט בגלוי בכוונה: הבוט מציג אותו ללקוחות, והעסק מדומה.

---

## 4. מה נבדק, ומה נשאר

| | מה | מצב |
|---|---|---|
| 1 | שאלה במבנה המדויק שהבוט שולח, עם הטוקן, לאתר בייצור | ✅ שעות, מחירים, ותשובה בערבית לשאלה בערבית |
| 2 | מספר הוואטסאפ באתר | ✅ בעברית, בערבית וברוסית |
| 3 | הבעלים והקבוצה לא הושפעו | ✅ בבדיקות האוטומטיות · ⏳ בטלפון אמיתי |
| 4 | **מקצה לקצה, מטלפון שאינו הבעלים** | ✅ שאלה בעברית ושאלה בערבית נענו, כל אחת בשפתה (25.9, 19:00 ו-19:03) · ⏳ תמונה, והבעלים והקבוצה |

### הבדיקה מטלפון אמיתי

זה הדבר היחיד שאי אפשר לבדוק מכאן. **מטלפון שאינו ברשימת הבעלים**, לשלוח לוואטסאפ של המוסך:

| לשלוח | מצופה |
|---|---|
| `מתי אתם פתוחים?` | השעות: א׳–ה׳ 07:00–17:00, ו׳ עד 12:00 |
| `אני רוצה לקבוע תור` | תשובה, ומתחתיה שורה עם הקישור לקביעת תור |
| תמונה כלשהי | "כאן אני קורא רק טקסט" והטלפון |
| `متى تفتحون؟` | אותן שעות, בערבית |

**ומהטלפון שלך**, הודעה רגילה: אמורה להגיע לעוזר האישי כמו תמיד. **ובקבוצה המשותפת**, "בוט ...": כמו תמיד.

**על איזה מספר זה רץ:** מספר נפרד, העסקי של בעל הפרויקט, ולא המספר האישי שלו. כרגע אף אחד אחר לא פונה אליו, ולכן הבוט נשאר דלוק עד אחרי ההדגמה. **ברגע שאנשים אמיתיים יתחילו לכתוב למספר הזה, כל מי שאינו ברשימת הבעלים יקבל תשובה של המוסך, והטקסט שלו יישלח לאתר.** בשלב הזה צריך לכבות את הבוט.

**איך מכבים את התשובות אחרי ההדגמה:** מוחקים את `GARAGE_ASK_URL` מהבוט ב-Vercel ופורסים מחדש. מאותו רגע זרים נדחים בשקט, כמו קודם.

---

## 5. השרשרת מול הלקוח (שלב ג') ✅ חיה בשני הצדדים (26.9)

| שלב | איך | מצב |
|---|---|---|
| **אישור תור** | אחרי Cal.com הלקוח מגיע ל-`/booked` ← כפתור לוואטסאפ עם הודעה כתובה ← הבוט עונה עם התאריך והשעה | ✅ בנוי · ⏳ הפניה ב-Cal.com (צעד 2 למטה) |
| **תזכורת ביום שלפני** | משימה יומית ב-Vercel (18:00 בקיץ), ובלוח של דניאל כפתור "לשלוח עכשיו" | ✅ חי, נבדק בייצור |
| **"מה המצב של הרכב שלי?"** | הבוט שולח את ה-HMAC של המספר; המסד מחשב אותו על הטלפונים ומחזיר רק את הרכבים של השולח | ✅ חי, נבדק בייצור, בעברית ובערבית |
| **הקישור לאישור תיקון** | "שליחה ללקוח" ← הקישור יוצא בוואטסאפ | ✅ חי |
| **"הרכב מוכן"** | "הרכב מוכן" ← הודעה | ✅ חי |
| **ביטול ושינוי מועד** | Make מעביר את האירוע; שינוי מועד מבטל את התור הקודם (טריגר במסד) | ✅ המסד · ⏳ המסנן ב-Make (צעד 1 למטה) |

### למי הבוט שולח: רק למי שכתב לנו ב-14 הימים האחרונים

המוסך בדוי, ולכן כל טלפון של "לקוח" בנתונים הוא נתון בדיקה. **`0500000001` מנתוני הדמו יכול להיות של אדם אמיתי**, אבל הוא מעולם לא כתב למוסך, ולכן לא יקבל כלום. **שום דבר לא נשמר בשביל זה:** הבוט קורא את השיחה ב-Green API ברגע השליחה.

**למה 14 ימים ולא 24 שעות:** תזכורת יוצאת יום לפני התור, ותורים נפתחים שבועיים קדימה. לקוח שכתב כשקבע תור חייב לקבל אותה. זה רופף מהכלל של ה-API הרשמי של WhatsApp Business (טקסט חופשי רק בתוך 24 שעות, ותבניות מאושרות אחרי זה); פריסה אמיתית על ה-API הרשמי תשתמש בתבניות. (הגרסה הראשונה השתמשה ברשימה קבועה של מספרים, ונזנחה כי אי אפשר לדעת מראש את המספר של הבוחן.)

### ⏳ שני צעדים שלך, בממשקים (כ-5 דקות)

**למה לא אני:** התרחיש ב-Make מכיל את הסיסמה הפנימית של קליטת התורים, וסיכמנו שאני לא קורא אותה. ב-Cal.com אין לי גישה.

**1. Make — לתת לביטולים ולשינויי מועד לעבור**

בתרחיש "מוסך לוי — תור חדש", על החיבור בין ה-Webhook למודול הראשון יש מסנן בשם "רק תור חדש עם מספר רישוי", עם שני תנאים: `triggerEvent` שווה ל-`BOOKING_CREATED`, ו-`plate` קיים.

- למחוק את התנאי של `triggerEvent`.
- **Add OR rule** ← `triggerEvent` · Text: Equal to · `BOOKING_CANCELLED`.
- לשמור.

התוצאה: תור חדש ושינוי מועד עוברים כמו קודם (יש להם מספר רישוי), וביטול עובר גם בלי. `intake_booking` כבר מטפלת בשלושתם. ה-Webhook ב-Cal.com כבר רשום לשלושת האירועים.

**2. Cal.com — להעביר את הלקוח לדף "התור נקבע"**

Event Types ← `drop-off` ← **Advanced** ← **Redirect on booking** ← `https://levi-garage.vercel.app/booked` ← לשמור.

התוצאה: אחרי קביעת תור, הלקוח רואה "התור נקבע ✅" וכפתור לוואטסאפ עם הודעה שכבר כתובה. הוא שולח, והבוט עונה עם אישור התור. מאותו רגע אפשר לשלוח לו תזכורת, קישור לאישור ו"הרכב מוכן".

### ✅ Cal.com מחובר ל-Make (אומת 26.9)

ה-Webhook ב-Cal.com מוגדר ומצביע לתרחיש ב-Make, על שלושה אירועים: הזמנה נוצרה, בוטלה, ומועד השתנה (צילום מסך, 26.9). **אף תור אמיתי לא הגיע לתרחיש**, פשוט כי אף אחד לא קבע תור מאז שהתרחיש נבנה ב-22.9. (בגרסה קודמת של המסמך נכתב שה-Webhook כנראה חסר. זו הייתה הסקה מהיעדר הרצות, והיא התבררה כשגויה.)

### מסלול הבוחן

**הטלפון שלכם הוא הלקוח, והמחשב הוא המוסך.** פרטי ההתחברות של דניאל ושל המכונאי נמצאים **במסמך ההגשה ולא בריפו**: המאגר ציבורי, והבוחן מקליד בטופס התור את הטלפון האמיתי שלו, שכל מי שמחובר כדניאל יכול לראות.

| | איפה | מה | מצופה |
|---|---|---|---|
| 1 | 📱 | קובעים תור ב-Cal.com (מהאתר, "קביעת תור"), עם הטלפון שלכם ועם הסכמה לוואטסאפ | דף "התור נקבע ✅" |
| 2 | 📱 | לוחצים "שליחת הודעה בוואטסאפ" ושולחים | **הבוט מאשר את התור, עם היום והשעה** |
| 3 | 📱 | שואלים "מה המצב של הרכב שלי?" | "יש לך תור ב-..." |
| 4 | 💻 | מתחברים כדניאל ← "לוח היום" ← "תורים בימים הקרובים" | התור מופיע, עם פרטי הרכב ממשרד התחבורה |
| 5 | 💻 | אם התור מחר: "לשלוח עכשיו את התזכורות למחר" | 📱 תזכורת עם השעה וקישור לביטול |
| 6 | 💻 | "הגיע מוקדם" | הכרטיס נפתח |
| 7 | 💻 | *(אופציונלי, בודק גם את פתרון 4)* להתחבר כמכונאי ← דף הליפט ← להקליט ממצא ("רפידות קדמיות שחוקות, מקורי 780, חלופי 520, מוכן ב-14:00"). חזרה לדניאל ← "שליחה ללקוח" | 📱 קישור לאישור ← מאשרים ← בכרטיס "הלקוח אישר" |
| 8 | 📱 | שואלים שוב "מה המצב?" | "בעבודה", עם הזמן המשוער |
| 9 | 💻 | "הרכב מוכן" | 📱 **"הרכב שלך מוכן לאיסוף ✅"** |

**ומה שאסור שיקרה:** כל הודעה על רכב אחר מהדמו לא יוצאת. בכרטיס מופיע "לא נשלחה ללקוח הודעה שהרכב מוכן: הלקוח לא כתב לנו בוואטסאפ ב-14 הימים האחרונים". וגם: שאלה על רכב של מישהו אחר ("מה הטלפון של 74815302?") — סירוב.

### קיצור דרך להדגמה מוקלטת

בלי Cal.com: שורה `DEMO_PHONE=9725XXXXXXXX` בקובץ המקומי (ואופציונלית `DEMO_NAME=...`), ואז:

```
cd levi-garage && node --env-file=.env.local scripts/seed-demo-notice.mjs
```

הסקריפט יוצר תור לסקודה FABIA עם הטלפון הזה, ולא מדפיס אותו. **גם כאן, קודם כותבים למוסך מהטלפון הזה.**

### אחרי שהטוקן המשותף מתחלף

המסד מחזיק עותק של `GARAGE_BOT_TOKEN`, כדי לזהות לקוח לפי המזהה של הבוט. אחרי כל הרצה של `wire-garage` שמייצרת טוקן חדש:

```
node --env-file=levi-garage/.env.local 03-rollout/solution-3-agent/scripts/sync-bot-token.mjs
```

### איך מכבים

- **רק את ההודעות היוזמות:** למחוק את `GARAGE_NOTIFY_TOKEN` מהבוט ב-Vercel ולפרוס מחדש. הכרטיסים ימשיכו לעבוד, ויציגו שההודעה לא יצאה.
- **את התזכורות בלבד:** למחוק את `CRON_SECRET` מהאתר. המשימה היומית נסגרת.
- **את כל ענף המוסך:** למחוק את `GARAGE_ASK_URL` מהבוט (סעיף 4).

**אחרי ההגשה:** למחוק את התורים והכרטיסים של הבוחנים. יש בהם טלפונים אמיתיים.
