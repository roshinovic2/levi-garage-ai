# פתרון 3 (רמה 3, סוכן AI + Make): עוזר הוואטסאפ של המוסך

> **בנייה בשלבים.** השלד (שלב א') הוא הצנרת שכל שאר המערכת נשענת עליה: **תור נקבע, הרכב מזוהה, והכול נשמר במקום אחד.** הבוט עצמו (שלב ב') יושב מעל הצנרת הזו.

## שלב א': קליטת תור (22.9.2026)

```
לקוח קובע תור ב-Cal.com (פתרון 2)
        │  Webhook: BOOKING_CREATED
        ▼
Make: "מוסך לוי — תור חדש (Cal.com ← משרד התחבורה)"
  ① קבלת התור
  ② מסנן: רק תור חדש, ורק אם יש מספר רישוי
  ③ שאילתה למאגר כלי הרכב של משרד התחבורה (data.gov.il, ציבורי, חינמי, בלי מפתח)
  ④ איחוד הכול לרשומה אחת: לקוח + שירות + יצרן, דגם, שנה, קוד מנוע, דלק, צמיגים ותוקף טסט
        │
        ▼
Supabase: טבלת bookings  ⏳ (החיבור בהמשך, ראו "מה חסר")
```

**למה זה חשוב למוסך:** המכונאי יודע מה מגיע מחר עוד לפני שהרכב נכנס לחניה. קוד המנוע (למשל CBZ) קובע איזה שמן ואיזה פילטר להכין, ומידת הצמיגים מאפשרת לבדוק מלאי. **רונית לא מקלידה שום דבר** (עיקרון "אפס הקלדה"). בנוסף, **תוקף הטסט** פותח הזדמנות להציע ללקוח הכנה לטסט.

### הבדיקה שבוצעה
| מה | תוצאה |
|---|---|
| תור לדוגמה ([test/cal-booking-created.sample.json](test/cal-booking-created.sample.json)), עם מספר רישוי שהוקלד **עם מקפים** (`82-153-76`) | ✅ התרחיש רץ בהצלחה (3 פעולות, 0.8 שניות). המקפים הוסרו אוטומטית |
| שאילתה ישירה למאגר על אותו מספר | ✅ סקודה צ'כיה, FABIA, 2012, מנוע CBZ, בנזין, צמיגים 185/60 R14, טסט בתוקף עד 31.12.2026 |

### קבצים
| קובץ | מה יש בו |
|---|---|
| [make/scenario-booking-intake.blueprint.json](make/scenario-booking-intake.blueprint.json) | התרחיש המלא, שאפשר לייבא ל-Make. כתובת ה-Webhook הוסרה |
| [sql/001_bookings.sql](sql/001_bookings.sql) | טבלת התורים, כולל RLS (אין גישה מהדפדפן) ועמודת הסכמה לוואטסאפ |
| [test/cal-booking-created.sample.json](test/cal-booking-created.sample.json) | תור לדוגמה בפורמט של Cal.com, עם **נתונים בדויים בלבד** |

### פרטיות ואבטחה
- **כתובת ה-Webhook לא מפורסמת בריפו.** מי שמחזיק בה יכול לשלוח "תור" מזויף. ⏳ בשלב הבא: סוד משותף (Secret) ב-Cal.com ובדיקה שלו בתרחיש.
- **הטבלה נעולה (RLS) ואין בה אף מדיניות לציבור.** רק צד השרת ניגש אליה.
- **`whatsapp_consent`:** בלי הסכמה מפורשת בטופס, הבוט לא שולח וואטסאפ. זה תנאי חוקי (חוק התקשורת, "חוק הספאם") וגם תנאי של WhatsApp.
- **נתוני הרכב ציבוריים** (מאגר פתוח של הממשלה). לא נשמר מספר השלדה, כי אין בו צורך (עיקרון מזעור המידע).

## מה חסר (הצעדים הבאים)
1. ⏳ **יצירת הטבלה ב-Supabase** והוספת שלב ⑤ "שמירה" לתרחיש.
2. ⏳ **חיבור ה-Webhook ב-Cal.com** לכתובת של Make, ובדיקה עם תור אמיתי.
3. ⏳ **ביטול ושינוי מועד** (`BOOKING_CANCELLED`, `BOOKING_RESCHEDULED`), כדי שהסטטוס בטבלה יתעדכן.
4. ⏳ **שלב ב': הבוט.** אישור תור בוואטסאפ, תזכורת ביום שלפני, "הרכב מוכן", ושאלות על מחיר וסטטוס עם מעבר לאדם.
